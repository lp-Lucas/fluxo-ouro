Layouts de rascunho PADRÃO do FLOW.

Solte aqui exatamente 3 imagens (PNG) com estes nomes:
  layout-1.png
  layout-2.png
  layout-3.png

Cada uma é um ESBOÇO/rascunho mostrando onde o texto e os elementos ficam
(posições, hierarquia). A IA usa SÓ o layout delas — as cores/estilo vêm da
identidade do projeto + da sua descrição. Não precisa ser bonito; é referência
de composição.

Proporção recomendada: 9:16 (vertical). Servidas em /flow-layouts/layout-N.png.
Para adicionar/renomear opções, edite FLOW_LAYOUT_TEMPLATES em shared/flow.ts.
