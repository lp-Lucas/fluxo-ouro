node_modules/
dist/
build/
.env
.env.local
*.log

# Python (whisper)
__pycache__/
*.pyc
.venv/
venv/

# Artefatos de mídia
uploads/
out/
*.mp4
*.mov
*.wav
*.m4a
# amostras de áudio da comparação da cadeia de tratamento (só a pasta da RAIZ —
# ancorado com "/" pra NÃO pegar backend/src/audio/ nem frontend/src/modules/audio/)
/audio/

# Dados de runtime — projetos criados no app (assets + exports, ~8 GB)
backend/projects/

# Documentos de negócio (planos, custos, playbooks) — não versionar
*.pdf

# Caches de build
.next/
*.tsbuildinfo
.vite/
.cache/
